{
  "version": 2,
  "env": {
    "PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD": "1"
  },
  "installCommand": "npm install && npx playwright install chromium",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.js"
    }
  ]
}
